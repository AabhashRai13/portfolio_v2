import 'package:flutter/widgets.dart';

class SkillBadge {
  const SkillBadge({
    required this.icon,
    required this.title,
    required this.color,
  });

  final IconData icon;
  final String title;
  final Color color;
}
