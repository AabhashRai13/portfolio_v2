import 'package:flutter/material.dart';

class CustomColor {
  // Primary Colors
  static const Color primary =
      Color(0xFFBFA181); // Light Brown (for highlights)
  static const Color secondary =
      Color(0xFFE6A4A4); // Muted Coral (optional accent)

  // Background Colors
  static const Color scaffoldBg = Color(0xFFFFF1E6); // Cream
  static const Color bgLight1 = Color(0xFFFFF1E6); // Cream
  static const Color bgLight2 = Color(0xFFEAD7C2); // Light Brown

  // Accent Colors
  static const Color accent = Color(0xFFE6A4A4); // Muted Coral
  static const Color grey = Color(0xFFD3D3D3); // Light Gray
  static const Color yellow = Color(0xFFFFF9DB); // Dusty Yellow

  // Text Colors
  static const Color textPrimary = Color(0xFF5C4033); // Dark Brown
  static const Color textSecondary = Color(0xFF6B4F3A); // Medium Brown
  static const Color textLight = Color(0xFFFFFFFF);
  static const Color pastelRed = Color(0xFFE6A4A4); // Muted Coral (optional)
}
